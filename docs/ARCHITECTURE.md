# 🏗️ ARQUITETURA VISUAL - admin.avila.inc

## 📊 VISÃO GERAL

```
                        ┌─────────────────────────────────┐
                        │     admin.avila.inc             │
                        │   (Render - Node.js 22)         │
                        └─────────────┬───────────────────┘
                                      │
                    ┌─────────────────┼─────────────────┐
                    │                 │                 │
          ┌─────────▼─────────┐  ┌───▼────┐  ┌────────▼────────┐
          │  Dashboard Web    │  │  API   │  │  Health Checks  │
          │   (Frontend)      │  │ Routes │  │   /health       │
          └───────────────────┘  └────┬───┘  └─────────────────┘
                                      │
                        ┌─────────────┼─────────────────┐
                        │             │                 │
                  ┌─────▼─────┐  ┌───▼────┐  ┌────────▼────────┐
                  │ Services  │  │ Config │  │   Middleware    │
                  └─────┬─────┘  └────────┘  └─────────────────┘
                        │
        ┌───────────────┼───────────────┬────────────────┐
        │               │               │                │
   ┌────▼────┐    ┌────▼────┐    ┌────▼────┐    ┌─────▼──────┐
   │ MongoDB │    │ GitHub  │    │ Stripe  │    │   Gmail    │
   │  Atlas  │    │   API   │    │   API   │    │ (3 contas) │
   └─────────┘    └─────────┘    └─────────┘    └────────────┘
```

---

## 🔄 FLUXO DE DADOS

### **Request Flow**

```
Cliente Browser
       │
       ▼
┌──────────────────┐
│  admin.avila.inc │
│   (HTTPS/SSL)    │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  Render Server   │
│  Port: 10000     │
└────────┬─────────┘
         │
    ┌────┴────┐
    │  CORS   │ ← Validação
    └────┬────┘
         │
    ┌────▼─────┐
    │   Auth   │ ← JWT Token
    └────┬─────┘
         │
    ┌────▼─────┐
    │  Routes  │ ← Express Router
    └────┬─────┘
         │
    ┌────▼─────┐
    │ Services │ ← Business Logic
    └────┬─────┘
         │
    ┌────▼─────┐
    │   APIs   │ ← External Services
    └──────────┘
```

---

## 🗄️ DATABASE ARCHITECTURE

```
MongoDB Atlas Cluster
│
├── avila_dashboard (Principal)
│   ├── users
│   ├── sessions
│   ├── logs
│   └── config
│
├── avila_gmail (Email)
│   ├── emails
│   │   ├── account: "email1@gmail.com"
│   │   ├── account: "email2@gmail.com"
│   │   └── account: "email3@gmail.com"
│   ├── threads
│   └── labels
│
└── avila_crm (CRM)
    ├── leads
    │   ├── nome
    │   ├── email
    │   ├── telefone
    │   ├── empresa
    │   ├── status
    │   └── observacoes
    ├── contacts
    └── companies
```

---

## 🔌 API INTEGRATIONS

```
┌──────────────────────────────────────────────┐
│          admin.avila.inc Server              │
└──────────────┬───────────────────────────────┘
               │
    ┌──────────┼──────────┬──────────┬─────────────┐
    │          │          │          │             │
┌───▼───┐  ┌──▼──┐  ┌────▼────┐  ┌──▼──┐  ┌─────▼─────┐
│GitHub │  │Stripe│  │  Gmail  │  │OpenAI│  │  MongoDB  │
│  API  │  │ API │  │   API   │  │ API │  │   Atlas   │
└───┬───┘  └──┬──┘  └────┬────┘  └──┬──┘  └─────┬─────┘
    │         │          │          │           │
    │         │          │          │           │
┌───▼────────────────────────────────────────────▼───┐
│              Response Aggregation                  │
└────────────────────────┬───────────────────────────┘
                         │
                    ┌────▼────┐
                    │  JSON   │
                    │Response │
                    └─────────┘
```

---

## 🎯 SERVICE LAYERS

```
┌─────────────────────────────────────────────┐
│            Presentation Layer               │
│  • Dashboard HTML                           │
│  • API Endpoints                            │
│  • Health Checks                            │
└─────────────────┬───────────────────────────┘
                  │
┌─────────────────▼───────────────────────────┐
│            Application Layer                │
│  • Express Routes                           │
│  • Middleware (Auth, CORS, Rate Limit)      │
│  • Error Handling                           │
└─────────────────┬───────────────────────────┘
                  │
┌─────────────────▼───────────────────────────┐
│             Business Layer                  │
│  • CRM Service                              │
│  • Gmail Service                            │
│  • Calendar Service                         │
│  • Campaign Service                         │
│  • E-Reader Service                         │
└─────────────────┬───────────────────────────┘
                  │
┌─────────────────▼───────────────────────────┐
│             Data Layer                      │
│  • MongoDB Service                          │
│  • GitHub API Client                        │
│  • Stripe API Client                        │
│  • Google Cloud API                         │
└─────────────────────────────────────────────┘
```

---

## 🔐 SECURITY LAYERS

```
Internet Request
       │
       ▼
┌──────────────┐
│  Cloudflare  │ ← DDoS Protection (opcional)
└──────┬───────┘
       │
       ▼
┌──────────────┐
│    SSL/TLS   │ ← HTTPS (Render automático)
└──────┬───────┘
       │
       ▼
┌──────────────┐
│   Helmet.js  │ ← Security headers
└──────┬───────┘
       │
       ▼
┌──────────────┐
│     CORS     │ ← Origin validation
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ Rate Limit   │ ← 100 req/15min
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ JWT Auth     │ ← Token validation
└──────┬───────┘
       │
       ▼
┌──────────────┐
│  Application │
└──────────────┘
```

---

## 📦 DEPLOYMENT PIPELINE

```
┌─────────────┐
│ Local Dev   │
│ npm run dev │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│  Git Commit │
│ git push    │
└──────┬──────┘
       │
       ▼
┌─────────────┐
│   GitHub    │
│ Repository  │
└──────┬──────┘
       │
       ▼ (Webhook)
┌─────────────┐
│   Render    │
│ Auto Deploy │
└──────┬──────┘
       │
       ├─► npm install
       ├─► node server.js
       └─► Health Check
              │
              ▼
       ┌─────────────┐
       │  Production │
       │ admin.avila │
       │    .inc     │
       └─────────────┘
```

---

## 🌐 DNS & DOMAIN FLOW

```
User Browser
     │
     │ Digita: admin.avila.inc
     │
     ▼
┌─────────────┐
│  Porkbun    │ ← DNS Provider
│  DNS Lookup │
└─────┬───────┘
      │
      │ CNAME: admin → avila-manager.onrender.com
      │
      ▼
┌─────────────┐
│   Render    │ ← Hosting
│  Load Bal.  │
└─────┬───────┘
      │
      │ Route to instance
      │
      ▼
┌─────────────┐
│ Node.js App │
│ Port 10000  │
└─────────────┘
```

---

## 💾 DATA FLOW - CRM

```
Cliente preenche formulário
         │
         ▼
POST /api/crm/lead
         │
         ▼
┌────────────────┐
│ Validação      │
│ • Nome         │
│ • Email        │
│ • Telefone     │
└────────┬───────┘
         │
         ▼
┌────────────────┐
│ Check Duplicate│
│ findOne()      │
└────────┬───────┘
         │
    ┌────┴────┐
    │ Existe? │
    └────┬────┘
         │
    Não  │  Sim
    ▼    │    ▼
┌────────┐  ┌────────┐
│ Create │  │ Error  │
│ Lead   │  │ 409    │
└───┬────┘  └────────┘
    │
    ▼
┌────────────────┐
│ MongoDB Insert │
│ avila_crm.leads│
└────────┬───────┘
         │
         ▼
┌────────────────┐
│ Return Lead ID │
│ { id: "..." }  │
└────────────────┘
```

---

## 📧 EMAIL SYNC FLOW

```
Gmail Account 1/2/3
         │
         ▼
┌────────────────┐
│  Gmail API     │
│  OAuth2        │
└────────┬───────┘
         │
         ▼
┌────────────────┐
│  Fetch Emails  │
│  • Inbox       │
│  • Sent        │
│  • Drafts      │
└────────┬───────┘
         │
         ▼
┌────────────────┐
│  Parse Data    │
│  • From/To     │
│  • Subject     │
│  • Body        │
│  • Date        │
└────────┬───────┘
         │
         ▼
┌────────────────┐
│ Store MongoDB  │
│ avila_gmail    │
│ .emails        │
└────────┬───────┘
         │
         ▼
┌────────────────┐
│ Index & Stats  │
│ • Total count  │
│ • By account   │
└────────────────┘
```

---

## 💳 PAYMENT FLOW - Stripe

```
Cliente → Checkout
         │
         ▼
POST /api/payments/create
         │
         ▼
┌────────────────┐
│ Stripe API     │
│ Create Payment │
│ Intent         │
└────────┬───────┘
         │
         ▼
┌────────────────┐
│ Return Secret  │
│ client_secret  │
└────────┬───────┘
         │
         ▼
Cliente confirma pagamento
         │
         ▼
┌────────────────┐
│ Webhook        │
│ /stripe/hook   │
└────────┬───────┘
         │
         ▼
┌────────────────┐
│ Update DB      │
│ • Status: paid │
│ • Invoice      │
└────────┬───────┘
         │
         ▼
┌────────────────┐
│ Send Receipt   │
│ via Email      │
└────────────────┘
```

---

## 🔍 MONITORING STACK

```
┌─────────────────────────────────────────────┐
│             Application                     │
└─────────────┬───────────────────────────────┘
              │
     ┌────────┼────────┬─────────────┐
     │        │        │             │
     ▼        ▼        ▼             ▼
┌────────┐ ┌──────┐ ┌──────┐ ┌────────────┐
│ Health │ │ Logs │ │Sentry│ │  Custom    │
│ Checks │ │      │ │(opt.)│ │  Metrics   │
└───┬────┘ └───┬──┘ └───┬──┘ └─────┬──────┘
    │          │        │          │
    │          │        │          │
    └──────────┴────────┴──────────┘
                   │
                   ▼
         ┌─────────────────┐
         │   Dashboard     │
         │   Monitoring    │
         └─────────────────┘
```

---

## 🚀 SCALABILITY DESIGN

```
Current (Free Tier)
┌─────────────────┐
│  Single Server  │
│  Render Free    │
│  512MB RAM      │
└─────────────────┘

Future (Scaled)
┌──────────────────────────────────┐
│      Load Balancer               │
└─────┬────────────┬───────────────┘
      │            │
┌─────▼─────┐ ┌───▼──────┐
│ Server 1  │ │ Server 2 │
│ Render    │ │ Render   │
└─────┬─────┘ └───┬──────┘
      │            │
      └─────┬──────┘
            │
      ┌─────▼──────┐
      │  MongoDB   │
      │  Cluster   │
      │  (Sharded) │
      └────────────┘
```

---

## 📊 PERFORMANCE METRICS

```
┌─────────────────────────────────────┐
│         Response Times              │
├─────────────────────────────────────┤
│ /health           │ < 50ms         │
│ /api/github/repos │ < 500ms        │
│ /api/crm/lead     │ < 200ms        │
│ /api/payments     │ < 1000ms       │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│         Resource Usage              │
├─────────────────────────────────────┤
│ RAM                │ ~200MB         │
│ CPU                │ < 5%           │
│ Disk               │ < 100MB        │
└─────────────────────────────────────┘
```

---

## 🎯 SYSTEM DIAGRAM

```
                    ┌────────────────┐
                    │   Internet     │
                    └────────┬───────┘
                             │
                    ┌────────▼────────┐
                    │  admin.avila    │
                    │     .inc        │
                    │  (Render SSL)   │
                    └────────┬────────┘
                             │
          ┌──────────────────┼──────────────────┐
          │                  │                  │
    ┌─────▼─────┐    ┌──────▼──────┐    ┌─────▼─────┐
    │   Web     │    │     API     │    │   Admin   │
    │ Dashboard │    │  Endpoints  │    │   Panel   │
    └───────────┘    └──────┬──────┘    └───────────┘
                            │
          ┌─────────────────┼─────────────────┐
          │                 │                 │
    ┌─────▼──────┐   ┌─────▼──────┐   ┌─────▼──────┐
    │  MongoDB   │   │   GitHub   │   │   Stripe   │
    │   Atlas    │   │     API    │   │    API     │
    │            │   │            │   │            │
    │ • Emails   │   │ • Repos    │   │ • Payments │
    │ • CRM      │   │ • Activity │   │ • Balance  │
    │ • Logs     │   │ • Stats    │   │ • Invoices │
    └────────────┘   └────────────┘   └────────────┘
```

---

**Arquitetura:** Monolítica → Microserviços (futuro)  
**Versão:** 2.1.0  
**Status:** Production Ready  
**Última Atualização:** 2024
